export enum ChatMessageType {
  TEXT = "TEXT",
  IMAGE = "IMAGE",
  JOIN = "JOIN",
  LEAVE = "LEAVE",
  INVITE = "INVITE",
  REMOVE = "REMOVE",
  FORM_ATTEMPT = "FORM_ATTEMPT",
}
export enum ChatMemberRole {
  MEMBER = "MEMBER",
  ADMIN = "ADMIN",
}
