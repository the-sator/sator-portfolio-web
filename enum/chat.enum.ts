export enum ChatMessageType {
  TEXT = "TEXT",
  IMAGE = "IMAGE",
  JOIN = "JOIN",
  LEAVE = "LEAVE",
  INVITE = "INVITE",
  REMOVE = "REMOVE",
}
export enum ChatMemberRole {
  MEMBER = "MEMBER",
  ADMIN = "ADMIN",
}
